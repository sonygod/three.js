import utest.Assert;
import utest.Test;

class WebGLStateTests {
  public static function main() {
    var test = new Test();

    test.describe("Renderers", () => {
      test.describe("WebGL", () => {
        test.describe("WebGLState", () => {
          // INSTANCING
          test.todo("Instancing", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          // PUBLIC STUFF
          test.todo("buffers", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("initAttributes", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("enableAttribute", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("enableAttributeAndDivisor", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("disableUnusedAttributes", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("enable", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("disable", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("getCompressedTextureFormats", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("useProgram", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("setBlending", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("setMaterial", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("setFlipSided", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("setCullFace", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("setLineWidth", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("setPolygonOffset", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("setScissorTest", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("activeTexture", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("bindTexture", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("compressedTexImage2D", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("texImage2D", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("scissor", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("viewport", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });

          test.todo("reset", () => {
            Assert.isTrue(false, "everything's gonna be alright");
          });
        });
      });
    });

    test.run();
  }
}