package;

import qunit.QUnit;

class WebGLClippingTest {
    public static function main() {
        QUnit.module("Renderers -> WebGL -> WebGLClipping", {
            // INSTANCING
            todo("Instancing", function(assert: qunit.Assert) {
                assert.ok(false, "everything's gonna be alright");
            });

            // PUBLIC STUFF
            todo("init", function(assert: qunit.Assert) {
                assert.ok(false, "everything's gonna be alright");
            });

            todo("beginShadows", function(assert: qunit.Assert) {
                assert.ok(false, "everything's gonna be alright");
            });

            todo("endShadows", function(assert: qunit.Assert) {
                assert.ok(false, "everything's gonna be alright");
            });

            todo("setState", function(assert: qunit.Assert) {
                assert.ok(false, "everything's gonna be alright");
            });
        });
    }
}