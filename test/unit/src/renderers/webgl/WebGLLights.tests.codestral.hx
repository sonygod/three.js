import qunit.QUnit;

class WebGLLightsTests {
    public function new() {
        QUnit.module("Renderers", () -> {
            QUnit.module("WebGL", () -> {
                QUnit.module("WebGLLights", () -> {
                    QUnit.todo("Instancing", (assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("setup", (assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("state", (assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });
                });
            });
        });
    }
}