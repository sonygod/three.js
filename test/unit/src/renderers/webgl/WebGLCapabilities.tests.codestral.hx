import qunit.QUnit;

class WebGLCapabilitiesTests {
    public static function main(): Void {
        QUnit.module("Renderers", () -> {
            QUnit.module("WebGL", () -> {
                QUnit.module("WebGLCapabilities", () -> {
                    // INSTANCING
                    QUnit.todo("Instancing", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    // PUBLIC STUFF
                    QUnit.todo("getMaxAnisotropy", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("getMaxPrecision", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("precision", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("logarithmicDepthBuffer", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("maxTextures", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("maxVertexTextures", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("maxTextureSize", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("maxCubemapSize", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("maxAttributes", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("maxVertexUniforms", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("maxVaryings", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("maxFragmentUniforms", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("vertexTextures", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("floatFragmentTextures", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    QUnit.todo("floatVertexTextures", (assert: QUnit.Assert) -> {
                        assert.ok(false, "everything's gonna be alright");
                    });
                });
            });
        });
    }
}