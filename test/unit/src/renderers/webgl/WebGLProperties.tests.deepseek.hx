package;

import js.Lib;

class TestWebGLProperties {

    static function main() {
        unittest.run();
    }

    @:test
    static function testInstancing() {
        assert(false);
    }

    @:test
    static function testGet() {
        assert(false);
    }

    @:test
    static function testRemove() {
        assert(false);
    }

    @:test
    static function testClear() {
        assert(false);
    }
}