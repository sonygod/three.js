package three.js.test.unit.src.renderers.webgl;

import three.renderers.webgl.WebGLMorphtargets;

class WebGLMorphtargetsTests {
    public static function main() {
        Tester.module("Renderers", () => {
            Tester.module("WebGL", () => {
                Tester.module("WebGLMorphtargets", () => {
                    // INSTANCING
                    Tester.todo("Instancing", (assert) => {
                        assert.ok(false, "everything's gonna be alright");
                    });

                    // PUBLIC STUFF
                    Tester.todo("update", (assert) => {
                        assert.ok(false, "everything's gonna be alright");
                    });
                });
            });
        });
    }
}