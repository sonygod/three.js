package three.test.unit.animation;

import haxe.unit.TestCase;
import three.animation.AnimationUtils;

class AnimationUtilsTest extends TestCase {
    public function new() {
        super();

        test("Animation", () => {
            test("AnimationUtils", () => {
                // PUBLIC

                todo(testConvertArray);
                todo(testIsTypedArray);
                todo(testGetKeyframeOrder);
                todo(testSortedArray);
                todo(testFlattenJSON);
                todo(testSubclip);
                todo(testMakeClipAdditive);
            });
        });
    }

    private function testConvertArray(assert:TestCase):Void {
        assert.ok(false, "everything's gonna be alright");
    }

    private function testIsTypedArray(assert:TestCase):Void {
        assert.ok(false, "everything's gonna be alright");
    }

    private function testGetKeyframeOrder(assert:TestCase):Void {
        assert.ok(false, "everything's gonna be alright");
    }

    private function testSortedArray(assert:TestCase):Void {
        assert.ok(false, "everything's gonna be alright");
    }

    private function testFlattenJSON(assert:TestCase):Void {
        assert.ok(false, "everything's gonna be alright");
    }

    private function testSubclip(assert:TestCase):Void {
        assert.ok(false, "everything's gonna be alright");
    }

    private function testMakeClipAdditive(assert:TestCase):Void {
        assert.ok(false, "everything's gonna be alright");
    }
}