package animation;

import js.QUnit;

class PropertyMixerTest {
    public static function main() : Void {
        QUnit.module("Animation", {
            setup: function() {
                // ...
            },
            teardown: function() {
                // ...
            }
        });

        QUnit.module("PropertyMixer", {
            // INSTANCING
            @QUnit.todo("Instancing")
            static function instancing(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            // PROPERTIES
            @QUnit.todo("binding")
            static function binding(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("valueSize")
            static function valueSize(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("buffer")
            static function buffer(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("cumulativeWeight")
            static function cumulativeWeight(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("cumulativeWeightAdditive")
            static function cumulativeWeightAdditive(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("useCount")
            static function useCount(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("referenceCount")
            static function referenceCount(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            // PUBLIC
            @QUnit.todo("accumulate")
            static function accumulate(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("accumulateAdditive")
            static function accumulateAdditive(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("apply")
            static function apply(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("saveOriginalState")
            static function saveOriginalState(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            },

            @QUnit.todo("restoreOriginalState")
            static function restoreOriginalState(assert: QUnit.Assert) : Void {
                assert.ok(false, "everything's gonna be alright");
            }
        });
    }
}