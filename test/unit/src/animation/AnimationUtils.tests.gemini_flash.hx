import haxe.unit.Assert;
import qunit.QUnit;

class AnimationUtilsTest {

    public static function main() {
        QUnit.module("Animation", function() {
            QUnit.module("AnimationUtils", function() {
                QUnit.todo("convertArray", function(assert:Assert) {
                    assert.ok(false, "everything's gonna be alright");
                });

                QUnit.todo("isTypedArray", function(assert:Assert) {
                    assert.ok(false, "everything's gonna be alright");
                });

                QUnit.todo("getKeyframeOrder", function(assert:Assert) {
                    assert.ok(false, "everything's gonna be alright");
                });

                QUnit.todo("sortedArray", function(assert:Assert) {
                    assert.ok(false, "everything's gonna be alright");
                });

                QUnit.todo("flattenJSON", function(assert:Assert) {
                    assert.ok(false, "everything's gonna be alright");
                });

                QUnit.todo("subclip", function(assert:Assert) {
                    assert.ok(false, "everything's gonna be alright");
                });

                QUnit.todo("makeClipAdditive", function(assert:Assert) {
                    assert.ok(false, "everything's gonna be alright");
                });
            });
        });
    }
}